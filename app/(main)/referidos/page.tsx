'use client';

import ReferidosPanel from '@/components/referrals/ReferidosPanel';

// Referidos: el enlace propio, quién se registró con él y cuánto se ha
// cobrado por cada uno.
export default function ReferidosPage() {
  return (
    <main>
      <ReferidosPanel />
    </main>
  );
}
